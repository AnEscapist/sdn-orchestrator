import axios from 'axios';

const state = {
};

const mutations = {
};

const actions = {
};

const getters = {
};

const hostInfoModule = {
  state,
  mutations,
  actions,
  getters
};

export default hostInfoModule;
